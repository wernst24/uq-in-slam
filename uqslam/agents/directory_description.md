# Directory Description: 
This directory stores **TensorFlow** / **PyTorch** models:

These contain neural network definitions used for training agents with reinforcement learning or supervised learning. Unlike physical models used in Gazebo or ROS (like SDF/URDF), these define computational architectures (e.g., actor-critic networks)